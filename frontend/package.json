{
  "name": "blinkit-review-analyzer-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "Stitch-exported static dashboard for Blinkit Review Analyzer (Vercel)",
  "scripts": {
    "build": "node scripts/inject-env.js",
    "dev": "node scripts/inject-env.js && npx serve public -p 3000",
    "preview": "npm run build && npx serve public -p 3000"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}
